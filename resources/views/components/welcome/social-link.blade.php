<a href="{{ $url }}" class="btn btn-neutral btn-icon" target="_blank">
    <span class="btn-inner--icon">
        <img src="{{ asset($icon) }}">
    </span>
    <span class="btn-inner--text">{{ $name }}</span>
</a>
